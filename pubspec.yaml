name: reworkmobile
description: "A new Flutter project."

publish_to: 'none' # Prevents accidental publishing

version: 1.0.0+1

environment:
  sdk: '>=3.6.0 <4.0.0' # Ensure correct version constraint format

dependencies:
  flutter:
    sdk: flutter
  http: ^1.3.0
  shared_preferences: ^2.5.1
  flutter_tts: ^3.4.0 
  permission_handler: ^12.0.0+1
  #http: any
  speech_to_text: ^7.0.0 # For voice-to-text functionality
  image_picker: ^1.1.2
  image: ^4.5.2
  google_fonts: ^6.1.0 
  web_socket_channel: ^3.0.2
  camera: ^0.11.1
  web_socket_client: ^0.2.1
  
  lucide_icons: ^0.257.0
  flutter_image_compress: ^2.4.0
  video_player: ^2.9.3
  webview_flutter: ^4.10.0
  file_picker: ^10.1.2
  cloud_firestore: ^5.6.7
  firebase_core: ^3.13.0
  firebase_auth: ^5.5.3
  firebase_messaging: ^15.2.5
  http_parser: ^4.0.2
  path_provider: ^2.1.5
  youtube_player_flutter: ^9.1.1
  flutter_launcher_icons: ^0.13.1
  mockito: ^5.3.0

flutter_icons:
  android: true
  ios: true
  image_path: "assets/ihear_logo.png"

dev_dependencies:
  integration_test:
    sdk: flutter
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/ihear_logo.png
    - assets/hand_wave.png
    - assets/logo.png
    - assets/bgatas.png
    
    - assets/scan_icon.png
    - assets/lesson_icon.png
    - assets/voice_icon.png
    - assets/background_lesson1.png
